/** Shared popover panel classes (the menu/panel that drops from a trigger),
 * used by AddFilterMenu so every dropdown-like panel in the app shares one
 * look and motion instead of being restyled per component. */
export const dropdownPanelClass =
  'animate-pop-in absolute top-[calc(100%+6px)] left-0 z-20 origin-top-left rounded-lg border border-paper-300 bg-white shadow-lg dark:border-dark-border dark:bg-dark-surface';
